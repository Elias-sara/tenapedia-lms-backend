// const express = require("express");
// const quizController = require("../controllersA/quizController");

// const router = express.Router();

// router.get("/", quizController.getQuizzes); // GET all quizzes
// router.post("/", quizController.createQuiz); // Create a quiz
// router.get("/:slug", quizController.getQuizBySlug); // Fetch quiz by slug
// router.put("/:slug", quizController.updateQuizBySlug); // Update quiz by slug
// router.delete("/:slug", quizController.deleteQuizBySlug); // Delete quiz by slug

// module.exports = router;
